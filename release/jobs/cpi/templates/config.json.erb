{
	"SocketPath": "<%= p('cpi.socket_path') %>",
	"Mbus": "<%= p('cpi.mbus') %>",
	"Blobstore": {
		"Type": "<%= p('cpi.blobstore.provider') %>",
		"Options": <%= JSON.dump(p('cpi.blobstore.options')) %>
	}
}